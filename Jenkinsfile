pipeline {
    agent {
        docker {
            
            sh 'echo oui'
        }
    }
    stages {
        stage('Build') { 
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}